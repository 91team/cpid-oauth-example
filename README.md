## Run

`yarn install`

`node src/server.js`

## Run inside docker

`docker build -t cpid-oauth-example:latest .`

`docker run -p 3000:3000 -it --rm cpid-oauth-example:latest`

## Open

http://localhost:3000
